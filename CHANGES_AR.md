# التعديلات المُنفَّذة على نظام RASTKT

هذا ملخص لكل بند طُلب تنفيذه، والملفات التي تغيّرت، وأي خطوة إعداد يدوية متبقية.

---

## 1) الالتباس بين زر إغلاق التذكرة وزر إغلاق نافذة المشاهدة
- زر إغلاق التذكرة أصبح نصّاً كاملاً واضحاً: **"إغلاق التذكرة" / "Close Ticket"** (بدل كلمة "إغلاق" المجرّدة التي تتشابه مع "إغلاق النافذة").
- زر الإغلاق السريع بقائمة التذاكر يحمل نفس التسمية الواضحة عبر tooltip.
- `templates/admin/dashboard.html`

## 2) اسم صاحب التعليق في إشعارات البريد
- كان يظهر دائماً "الدعم الفني" لأي تعليق من موظف. الآن يظهر **اسم الموظف الفعلي** الذي كتب التعليق.
- `utils/email_utils.py` → `send_new_comment_notification`

## 3) تعديل حسابات المستخدمين وكلمات المرور والصلاحيات
- زر تعديل ✏️ جديد بجانب كل موظف بقائمة "الموظفون" يفتح نافذة تعديل: الاسم، البريد، **كلمة مرور جديدة (اختياري)**، والدور.
- تمت إضافة شرح واضح لكل دور (دعم فني / مسؤول نظام / مدير عام) يظهر عند الإضافة وعند التعديل، ليستعين به مدير النظام عند التعيين.
- الواجهة الخلفية (`PUT /admin/api/staff/<id>`) كانت تدعم هذا فعلياً؛ التعديل هنا هو توفير واجهة مستخدم له.
- `templates/admin/dashboard.html`

## 4) رابط الاستعلام المباشر لصاحب التذكرة
- أُضيف زر/رابط "متابعة التذكرة مباشرة" داخل إشعارات: استلام التذكرة، إغلاقها، إعادة فتحها، دمجها، والتعليق عليها من الموظف.
- `utils/email_utils.py`

## 5) رابط لوحة التحكم للموظف المعيَّن
- أُضيف زر/رابط "فتح لوحة التحكم" داخل إشعارات: تعيين التذكرة، التعليق عليها من المعلم، تغيير الأولوية، وتذكير التذاكر غير المعالجة.
- `utils/email_utils.py`

## 6) حالة "بانتظار الرد" (Waiting)
- الحالة موجودة مسبقاً باسم `waiting` (بانتظار الرد / Waiting)، وقد جرى التأكيد على استثنائها صراحةً من ميزة التذكير التلقائي (البند التالي) — أي أنه طالما التذكرة بهذه الحالة، لن يُرسل أي تذكير آلي حتى يُغيَّر الوضع يدوياً.

## 7) تذكير تلقائي بالتذاكر غير المعالجة
ميزة جديدة كاملة:
- تبويب **الإعدادات** فيه الآن بطاقة "التذكير التلقائي بالتذاكر غير المعالجة": تفعيل/تعطيل + عدد الساعات (قابل للتخصيص، الافتراضي 24) + زر "فحص الآن" للتجربة الفورية.
- عند الفحص: أي تذكرة (غير "بانتظار الرد" وغير مغلقة) مرّت عليها المدة المحددة دون أي تعليق/إجراء منذ إنشائها → يُرسل بريد للموظف المعيَّن ولكل حسابات "مسؤول نظام/مدير عام"، نصّه: "لم تتم معالجة الطلب رقم # حتى الآن".
- لا يتكرر الإشعار لنفس التذكرة إلا إذا طرأ عليها تعليق أو تغيير حالة ثم عادت لتبقى بلا حركة مجدداً.
- **خطوة إعداد مطلوبة منك:** لا يوجد مجدوِل مهام (scheduler) داخل التطبيق نفسه على Render، لذلك أضفنا مساراً خاصاً `POST /cron/check-reminders?secret=...` يستدعيه مجدوِل خارجي (مثل **Render Cron Job**) كل 15–30 دقيقة مثلاً. يجب:
  1. تعيين متغيّر بيئة `CRON_SECRET` بقيمة سرّية على Render.
  2. إنشاء Cron Job يستدعي: `POST https://<your-app>.onrender.com/cron/check-reminders?secret=<CRON_SECRET>` على فترات دورية.
  - بدون هذه الخطوة، زر "فحص الآن" اليدوي بالإعدادات سيبقى يعمل، لكن الفحص التلقائي الدوري لن يحدث من تلقاء نفسه.
- `models.py` (عمود `reminder_sent` جديد)، `utils/reminder_utils.py` (جديد)، `blueprints/admin.py`، `app.py`

## 8) صفحة Monitoring (بدون تسجيل دخول)
- صفحة جديدة مستقلة على المسار **`/monitoring`**: تبويبان فقط — "التذاكر" (قائمة كاملة مع بحث وفلترة حسب الحالة وعرض تفاصيل عند الضغط) و"التقارير" (إجماليات حسب الحالة/الأولوية/التصنيف ومتوسط زمن الإغلاق).
- لا تسجيل دخول، ولا أي خيار إدارة آخر (لا تعديل، لا حذف، لا إعدادات).
- حفاظاً على الخصوصية: لا تُعرض هذه الصفحة بريد أو جوال المعلم، ولا الملاحظات الداخلية.
- `templates/monitoring.html` (جديد)، `blueprints/public.py`، `models.py` (دالة `to_monitor_dict`)

## 9) مراجعة الترجمة ومحاذاة الخط (عربي/إنجليزي)
- تم فحص كل النصوص الجديدة المُضافة وتغطيتها بالكامل في قاموسي الترجمة (لوحة التحكم وصفحة المراقبة)، والتأكد من عدم وجود نصوص إنجليزية بلا مقابل عربي أو العكس.
- صفحة Monitoring الجديدة تتبع نفس منطق `dir="rtl"/"ltr"` والخط (Tajawal عربي / Inter إنجليزي) المستخدم في باقي الموقع.
- لم يُعثر على مشاكل محاذاة إضافية تستدعي تعديلاً في الصفحات القائمة.

## 10) إغلاق أي نافذة منبثقة بزر Esc أو بالضغط خارجها
- تمت إضافة معالج عام لمفتاح **Esc** يُغلق أي نافذة منبثقة مفتوحة (نافذة تفاصيل التذكرة، ونافذة تعديل حساب الموظف الجديدة).
- الضغط خارج صندوق النافذة (على الخلفية الداكنة) يُغلقها أيضاً — وهو نفس السلوك في صفحة Monitoring الجديدة.
- `templates/admin/dashboard.html`، `templates/monitoring.html`

## 11) وصول التعليقات للمعنيين حسب داخلي/عام
- **تعليق داخلي (خاص بالموظفين):** الآن يصل بريد إشعار لبقية حسابات "مسؤول نظام/مدير عام" (غير كاتب التعليق) — سابقاً كان لا يُرسل أي إشعار عن الملاحظات الداخلية إطلاقاً.
- **رد عام من المعلم على تذكرة بلا موظف معيَّن:** سابقاً لا يصل لأي أحد. الآن يصل تلقائياً لكل حسابات "مسؤول نظام/مدير عام" كحل احتياطي.
- `blueprints/admin.py`، `blueprints/public.py`، `utils/email_utils.py` (دالة جديدة `send_internal_note_notification`)

## 12) رسالة تأكيد للأزرار الحساسة
- أُضيف تأكيد (`confirm`) قبل: **إغلاق التذكرة، إعادة فتحها، أرشفتها، ودمجها** بتذكرة أخرى (كان الدمج بلا أي تأكيد نهائي قبل التنفيذ).
- الأزرار الحساسة الأخرى (تعطيل/حذف موظف، حذف معلم، حذف تصنيف) كانت تحتوي بالفعل على تأكيد.
- `templates/admin/dashboard.html`

---

## ملاحظة مهمة حول النشر
لا تملك الجلسة صلاحية push إلى مستودعكم على GitHub (`AcsVe/RASTKT`) مباشرة. كل التعديلات أعلاه منفّذة ومُختبرة محلياً (فحص بايثون، اختبار مسارات API فعلي عبر Flask test client، وفحص صلاحية جافاسكربت للصفحتين المعدَّلتين). تم تسليمها كملف مضغوط لكامل المشروع المعدَّل — فقط استخرجه فوق نسخة العمل لديك (أو ادفعه إلى GitHub يدوياً) وشغّل:

```
pip install -r requirements.txt
```

قاعدة البيانات ستُحدَّث تلقائياً بعمود `reminder_sent` الجديد عند أول تشغيل (نفس آلية الترحيل التلقائي المستخدمة أصلاً في المشروع).
